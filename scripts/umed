#!/bin/sh

# This script unmounts encrypted drives

term='st -f monospace:size=16'

drive=$(find /mnt -type d -maxdepth 3 2>/dev/null)
mountpoint=$(echo "$drive" | dmenu -i -p "Select drive to unmount:")
$term sudo umount -R $mountpoint
sudo cryptsetup close drive
